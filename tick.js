#!/usr/bin/env node

tick = function(base, nticks, max){
  // Place tick marks given a max tick number.
  // The location is termined by an exponent base
  // and by the number of ticks.
  Math.pow
}
